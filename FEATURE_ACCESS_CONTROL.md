# 🔐 アクセス制御機能 - 開発予定

**作成日**: 2025-10-28  
**ステータス**: 企画中  
**優先度**: 中

---

## 📋 概要

特定のアクセスIDを持つユーザーが、許可されたキャストのみを操作できるようにする機能。複数人での運営や、外部スタッフへの限定的なアクセス権付与を実現。

---

## 🎯 目的・ユースケース

### 主な目的
1. **複数人運営**: 担当キャストごとにアクセス権を分離
2. **外部スタッフ管理**: 限定的な権限で外部ライターに依頼
3. **セキュリティ強化**: 不要なキャストへのアクセスを制限
4. **監査ログ**: 誰がどのキャストを操作したか追跡

### ユースケース例
```
例1: 外部ライター「田中さん」
  → キャストA, Bのみ操作可能
  → 投稿管理・キャスト情報閲覧のみ許可

例2: 管理者「山田さん」
  → 全キャスト操作可能
  → システム設定・アクセスID管理も可能

例3: 期間限定スタッフ
  → 有効期限付きアクセスID発行
  → 期限切れ後は自動無効化
```

---

## 🏗️ アーキテクチャ設計

### 推奨実装パターン: **パターンB（ログイン画面方式）**

#### 理由
- ✅ セキュリティが高い（パスワード認証）
- ✅ UXが良好（ログイン画面で明確）
- ✅ 管理画面で動的にID追加・削除可能
- ✅ セッション管理で自動ログアウト対応

---

## 🗄️ データベース設計

### 新規テーブル: `access_ids`

```sql
CREATE TABLE IF NOT EXISTS access_ids (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    access_id TEXT UNIQUE NOT NULL,           -- アクセスID（例: "user_001", "writer_tanaka"）
    password_hash TEXT NOT NULL,              -- パスワード（ハッシュ化）
    name TEXT NOT NULL,                       -- 表示名（例: "田中 花子"）
    allowed_cast_ids TEXT,                    -- 許可キャストID（カンマ区切り: "1,3,5"）
    is_admin BOOLEAN DEFAULT 0,               -- 管理者フラグ（全キャスト操作可能）
    permissions TEXT DEFAULT 'read,write',    -- 権限（例: "read", "read,write", "admin"）
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    expires_at DATETIME,                      -- 有効期限（NULL=無期限）
    is_active BOOLEAN DEFAULT 1,              -- 有効/無効フラグ
    last_login_at DATETIME,                   -- 最終ログイン日時
    notes TEXT                                -- 備考（管理用メモ）
)
```

### 拡張テーブル: `access_logs`（監査ログ）

```sql
CREATE TABLE IF NOT EXISTS access_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    access_id TEXT NOT NULL,
    cast_id INTEGER,
    action TEXT NOT NULL,                     -- 操作内容（例: "login", "create_post", "edit_cast"）
    details TEXT,                             -- 詳細（JSON形式）
    ip_address TEXT,                          -- IPアドレス
    user_agent TEXT,                          -- ブラウザ情報
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
)
```

---

## 🔧 実装機能一覧

### フェーズ1: 基本機能（MVP）
- [ ] **ログイン画面**
  - アクセスID + パスワード入力
  - 認証処理（パスワードハッシュ検証）
  - セッション管理（`st.session_state`）
- [ ] **サイドバー制御**
  - ログイン情報表示
  - 許可されたキャストのみ表示
  - ログアウトボタン
- [ ] **アクセスID管理画面**（システム設定に追加）
  - ID作成（アクセスID、パスワード、名前、許可キャスト選択）
  - ID一覧表示
  - ID編集・削除
- [ ] **管理者権限**
  - `is_admin=1`の場合、全キャスト操作可能
  - アクセスID管理画面へのアクセス権限

### フェーズ2: セキュリティ強化
- [ ] **パスワードハッシュ化**（bcrypt使用）
- [ ] **セッションタイムアウト**（30分無操作で自動ログアウト）
- [ ] **有効期限チェック**（`expires_at`超過で自動無効化）
- [ ] **IP制限**（オプション: 特定IPからのみアクセス許可）

### フェーズ3: 監査・分析
- [ ] **アクセスログ記録**
  - ログイン/ログアウト
  - 投稿作成/編集/削除
  - キャスト情報変更
- [ ] **ログ閲覧画面**（管理者のみ）
  - ユーザー別アクセス履歴
  - 操作ログのフィルタリング・検索

### フェーズ4: 高度な権限管理
- [ ] **詳細権限設定**
  - `permissions`: "read"（閲覧のみ）、"read,write"（編集可）、"admin"（全権限）
  - ページ別アクセス制御（投稿管理のみ、キャスト管理は不可、など）
- [ ] **ロール管理**
  - プリセットロール（閲覧者、編集者、管理者）
  - カスタムロール作成

---

## 🎨 UI設計

### 1. ログイン画面
```
━━━━━━━━━━━━━━━━━━━━━━━
    AIcast Room
    🔐 ログイン
━━━━━━━━━━━━━━━━━━━━━━━

アクセスID: [_______________]
パスワード: [_______________]

          [ログイン]

━━━━━━━━━━━━━━━━━━━━━━━
```

### 2. サイドバー（ログイン後）
```
━━━━━━━━━━━━━━━━━━━━━━━
AIcast room

━━━━━━━━━━━━━━━━━━━━━━━
🔐 ログイン情報
  ID: user_001 (田中 花子)
  権限: 編集者
  [ログアウト]
━━━━━━━━━━━━━━━━━━━━━━━

🎭 アカウント選択
  運営中のアカウント
  ▼ 156_syoy (そよ)
  
  ✅ 156_syoy (そよ)
  年齢: 21歳
  
  💡 許可されたアカウント: 2件
━━━━━━━━━━━━━━━━━━━━━━━
```

### 3. アクセスID管理画面（システム設定）
```
━━━━━━━━━━━━━━━━━━━━━━━
⚙️ システム設定
━━━━━━━━━━━━━━━━━━━━━━━

タブ: [🔐 アクセスID管理] [🔧 アプリ設定]

━━━━━━━━━━━━━━━━━━━━━━━
➕ 新規アクセスID作成
━━━━━━━━━━━━━━━━━━━━━━━

アクセスID*: [_______________]
パスワード*: [_______________]
表示名*: [_______________]
管理者権限: [ ] 有効

許可するキャスト:
  [✓] 156_syoy (そよ)
  [ ] Hiranonorico (平野のリコ)
  [✓] tanaka_hana (田中 花音)

有効期限: [____] 無期限 [ ]

         [作成]

━━━━━━━━━━━━━━━━━━━━━━━
📋 登録済みアクセスID
━━━━━━━━━━━━━━━━━━━━━━━

user_001 (田中 花子)
  権限: 編集者 | キャスト: 2件 | 最終ログイン: 2025-10-28 14:30
  [編集] [削除]

admin (管理者)
  権限: 管理者 | キャスト: 全て | 最終ログイン: 2025-10-28 15:00
  [編集] [削除]
━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 🔍 セキュリティ考慮事項

### 実装必須
1. **パスワードハッシュ化**: bcryptまたはargon2使用
2. **セッション管理**: 適切なタイムアウト設定
3. **HTTPS必須**: 本番環境では必須
4. **SQLインジェクション対策**: プレースホルダー使用（既存実装で対応済み）

### 推奨実装
1. **ログイン試行制限**: 5回失敗でロックアウト
2. **2FA（二要素認証）**: 高セキュリティが必要な場合
3. **監査ログ**: 全操作を記録
4. **パスワードポリシー**: 最低8文字、大小英数記号混在

---

## 📦 必要な追加パッケージ

```bash
# パスワードハッシュ化
pip install bcrypt

# セッション管理（オプション）
pip install streamlit-authenticator

# 2FA（オプション）
pip install pyotp qrcode
```

---

## 🚀 実装の流れ

### ステップ1: データベース準備
1. `access_ids`テーブル作成
2. 管理者アカウント初期登録
3. テストデータ投入

### ステップ2: ログイン機能実装
1. ログイン画面UI作成
2. 認証ロジック実装
3. セッション管理実装

### ステップ3: サイドバー制御
1. ログイン情報表示
2. キャスト選択フィルタリング
3. ログアウト機能

### ステップ4: 管理画面実装
1. アクセスID作成UI
2. 一覧表示・編集・削除機能
3. 権限管理UI

### ステップ5: テスト
1. 各権限レベルでの動作確認
2. セキュリティテスト
3. エッジケーステスト

---

## 🎯 期待される効果

### 運営効率化
- ✅ 担当者ごとに適切な権限を付与
- ✅ 外部スタッフへの安全なアクセス提供
- ✅ 誤操作のリスク低減

### セキュリティ向上
- ✅ 不要なキャストへのアクセス制限
- ✅ 操作ログによる監査
- ✅ 有効期限による自動無効化

### スケーラビリティ
- ✅ 複数人での大規模運営に対応
- ✅ 将来的なSaaS化への基盤

---

## 📝 備考

### 代替案・検討事項
1. **Streamlit Cloud Secrets**: 環境変数でシンプルに管理
2. **外部認証サービス**: Auth0、Firebase Authenticationなど
3. **ロール管理の粒度**: ページ単位、機能単位でさらに細かく制御

### 参考資料
- [Streamlit Authentication Best Practices](https://docs.streamlit.io/knowledge-base/deploy/authentication-without-sso)
- [bcrypt Documentation](https://github.com/pyca/bcrypt/)
- [streamlit-authenticator](https://github.com/mkhorasani/Streamlit-Authenticator)

---

## 🔄 更新履歴

- **2025-10-28**: 初版作成（企画段階）

---

**次のステップ**: 要件定義の詳細化、技術選定、プロトタイプ開発
