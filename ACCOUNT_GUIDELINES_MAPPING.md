# アカウント運営指針 - テキスト項目とCSVフィールド対応表

## 📋 概要

このドキュメントは、アカウント運営指針ドキュメントのテキスト項目と、データベース・CSVフィールドの対応関係を示します。

---

## 🗂️ データ構造

### 1. ペルソナCSV（詳細ペルソナ）

**CSVヘッダー:**
```csv
ID,名前,年齢,アーキタイプ分類,職業,居住地,家族構成,人物像／象徴的な一言,X利用の目的と動機,行動と利用パターン,関心のあるトピック,主なフォロー対象,プラットフォームへの不満とペインポイント,ブランドとの関わり方と購買への影響
```

**テーブル:** `persona_detailed`

| CSV列番号 | CSVヘッダー | DBカラム名 | UIフィールド名 | 備考 |
|---------|---------|----------|------------|------|
| 0 | ID | - | - | キャストIDとして使用 |
| 1 | 名前 | - | - | キャスト名として使用 |
| 2 | 年齢 | - | 年齢 | キャスト基本情報 |
| 3 | アーキタイプ分類 | archetype | アーキタイプ | - |
| 4 | 職業 | occupation | 職業 | - |
| 5 | 居住地 | residence | 居住地 | - |
| 6 | 家族構成 | family_structure | 家族構成 | - |
| 7 | 人物像／象徴的な一言 | symbolic_quote | 象徴的な一言 | - |
| 8 | X利用の目的と動機 | x_usage_purpose | X利用目的 | - |
| 9 | 行動と利用パターン | behavior_pattern | 行動パターン | - |
| 10 | 関心のあるトピック | interested_topics | 関心トピック | - |
| 11 | 主なフォロー対象 | main_follow_targets | 主なフォロー対象 | 新規追加 |
| 12 | プラットフォームへの不満とペインポイント | platform_pain_points | プラットフォーム不満 | - |
| 13 | ブランドとの関わり方と購買への影響 | brand_relationship | ブランド関係 | - |

---

### 1.5. Xアカウントサンプル（ランダム3つ選択）

**CSVフォーマット:**
```csv
アカウントID,ニックネーム
Honne_no_Maruko,本音のまる子
Uragawa_no_Sazae,裏側のサザエさん
...
```

**テーブル:** `persona_detailed`

| DBカラム名 | UIフィールド名 | 抽出方法 | 備考 |
|----------|------------|---------|------|
| x_sample_id_1 | XサンプルID #1 | ランダム選択 | リストから3つランダム抽出 |
| x_sample_name_1 | Xサンプルニックネーム #1 | ランダム選択 | 対応するニックネーム |
| x_sample_id_2 | XサンプルID #2 | ランダム選択 | - |
| x_sample_name_2 | Xサンプルニックネーム #2 | ランダム選択 | - |
| x_sample_id_3 | XサンプルID #3 | ランダム選択 | - |
| x_sample_name_3 | Xサンプルニックネーム #3 | ランダム選択 | - |

**抽出ロジック:**
- アカウントID/ニックネームのCSVリストから`random.sample()`で3つランダム選択
- 毎回の抽出で異なるアカウントが選ばれる可能性がある
- セッションステートに保存され、UIフィールドに自動反映

---

### 2. アカウント運営指針（ミッション・戦略）

**テーブル:** `account_mission`

| DBカラム名 | UIフィールド名 | 対応するテキスト形式（複数パターン） |
|----------|------------|--------------------------|
| mission | 運営ミッション | ① `### 運営ミッション:`<br>② `### 私たちのミッションと3つの原則`<br>③ `### **私たちのミッション...**` |
| persona_design | ペルソナ設計意図 | ① `### ペルソナ設計意図:`<br>② `### 第1章：...`<br>③ `### **第1章：...**` |
| content_strategy | コンテンツ戦略 | ① `### コンテンツ戦略:`<br>② `### 第2章：...`<br>③ `### **第2章：...**` |
| final_goal | 最終目標 | ① `### 最終目標:`<br>② `### 第3章：...`<br>③ `### **第3章：...**` |
| additional_notes | 補足事項 | 手動入力のみ |

**抽出の優先順位:**
各項目は**3段階のフォールバック処理**で抽出されます：
1. 明示的ラベル形式（例: `### 運営ミッション:`）
2. 章立て形式（例: `### 第1章：...`）
3. 太字章立て形式（例: `### **第1章：...**`）

---

### 3. サンプルプロフィール

**テーブル:** `sample_profiles`

| DBカラム名 | UIフィールド名 | 対応するテキスト形式（複数パターン） |
|----------|------------|--------------------------|
| profile_text | サンプルプロフィール | ① `**プロフィール:**`（太字）<br>② `プロフィール:`（通常） |

**抽出の優先順位:**
1. 太字形式（`**プロフィール:**`）を優先検索
2. 見つからなければ通常形式（`プロフィール:`）を検索

**例:**
```
**プロフィール:**
結婚10年、夫はもう私を見てくれない。鏡の中の疲れた顔の女は、誰？ 
もう一度「私」に戻りたくて、禁断の果実を、食べた。｜引用RTには、必ず返信します。
```

---

### 4. サンプル投稿

**CSVヘッダー:**
```csv
Category,Post_Content
```

**テーブル:** `sample_posts`

| CSV列 | DBカラム名 | 説明 |
|------|----------|------|
| Category | category | 投稿カテゴリ（例: 告白録、妻の仮面、女の本音、魂の対話） |
| Post_Content | post_content | 投稿本文（ダブルクォート内のカンマ対応） |

**抽出パターン:**
```
Category,Post_Content
"告白録","今日、彼に会った。..."
"妻の仮面","夫と晩ご飯を食べた。..."
```

---

## 🔄 テキスト一括インポート機能

### 入力方法
1. キャスト管理 → 編集 → 運営指針タブ
2. 「テキスト一括インポートフォーム」を展開
3. アカウント運営指針の全文を貼り付け
4. 「🔄 テキストから自動抽出して反映」ボタンをクリック

### 抽出される項目
- ✅ ペルソナ情報（CSV形式）→ 詳細ペルソナフォームに自動入力
- ✅ Xアカウントサンプル（CSV形式）→ ランダムに3つ選択して自動入力
- ✅ サンプルプロフィール → サンプルプロフィールフィールドに自動入力
- ✅ 運営ミッション → 運営ミッションフィールドに自動入力
- ✅ ペルソナ設計意図（第1章） → ペルソナ設計意図フィールドに自動入力
- ✅ コンテンツ戦略（第2章） → コンテンツ戦略フィールドに自動入力
- ✅ 最終目標（第3章） → 最終目標フィールドに自動入力
- ✅ サンプル投稿（CSV形式） → プレビュー表示 + 一括インポートボタン

---

## 📝 テキストフォーマット例

### 完全なアカウント運営指針フォーマット

```markdown
## **アカウント運営指針**

# **私たちが目指すもの：〜〜〜**

### 私たちのミッションと3つの原則

このアカウントは、〜〜〜を目指します。

1. 原則1
2. 原則2
3. 原則3

### 第1章：私たちのペルソナと第一印象の設計

#### 1.1. アイデンティティ
〜〜〜

#### 1.2. プロフィール
〜〜〜

**プロフィール:**
実際のプロフィール文がここに入ります。

### 第2章：私たちのコンテンツ戦略

#### 2.1. コンテンツの柱
〜〜〜

#### 2.2. トーン＆マナー
〜〜〜

### 第3章：私たちが目指すゴール

最終目標の説明がここに入ります。

## **ペルソナ設定**

ID,名前,年齢,アーキタイプ分類,職業,居住地,家族構成,人物像／象徴的な一言,X利用の目的と動機,行動と利用パターン,関心のあるトピック,主なフォロー対象,プラットフォームへの不満とペインポイント,ブランドとの関わり方と購買への影響
1,美咲,28,共感者/正直者,一般企業の営業事務,東京都中野区,一人暮らし,"建前ばかりの毎日に疲れた。もっと本音で、人間臭く生きたい。",仕事や人間関係のストレスを忘れさせてくれる〜,通勤中や寝る前に〜,#猫 #イケメン俳優,自分と似たような価値観を持つ匿名の個人アカウント,仕事の愚痴やネガティブな投稿ばかりで〜,信頼しているアカウント運営者が〜

## **サンプル投稿**

Category,Post_Content
"告白録","今日、彼に会った。..."
"妻の仮面","夫と晩ご飯を食べた。..."
"女の本音","綺麗だねって言われた。..."
"魂の対話","みんなは、どんな時に『女』を実感する？"
```

---

## ⚙️ セッションステートキー

抽出されたデータは以下のセッションステートキーに保存されます：

### ペルソナ詳細
- `parsed_age_{cast_id}`
- `parsed_archetype_{cast_id}`
- `parsed_occupation_{cast_id}`
- `parsed_residence_{cast_id}`
- `parsed_family_{cast_id}`
- `parsed_quote_{cast_id}`
- `parsed_x_purpose_{cast_id}`
- `parsed_behavior_{cast_id}`
- `parsed_topics_{cast_id}`
- `parsed_follow_{cast_id}`
- `parsed_pain_{cast_id}`
- `parsed_brand_{cast_id}`

### Xアカウントサンプル（ランダム3つ選択）
- `parsed_x_sample_id_1_{cast_id}` - XサンプルID #1
- `parsed_x_sample_name_1_{cast_id}` - Xサンプルニックネーム #1
- `parsed_x_sample_id_2_{cast_id}` - XサンプルID #2
- `parsed_x_sample_name_2_{cast_id}` - Xサンプルニックネーム #2
- `parsed_x_sample_id_3_{cast_id}` - XサンプルID #3
- `parsed_x_sample_name_3_{cast_id}` - Xサンプルニックネーム #3

### 運営指針
- `parsed_mission_{cast_id}`
- `parsed_persona_design_{cast_id}`
- `parsed_content_strategy_{cast_id}`
- `parsed_final_goal_{cast_id}`

### その他
- `parsed_profile_{cast_id}` - サンプルプロフィール
- `parsed_posts_csv_{cast_id}` - サンプル投稿CSV

---

## 🔍 正規表現パターン詳細

### ペルソナCSV抽出

**正規表現:**
```python
r'ID,名前,年齢.*?\n([A-Za-z0-9]+,.+?)(?=\n\n|---|\Z)'
```

**対応するID形式:**
- 数字のみ: `1`, `123`
- 英字+数字: `P001`, `CAST001`
- 大文字/小文字: `P001`, `p001`, `Cast01`

---

### Xアカウントサンプル抽出（ランダム3つ選択）

**正規表現:**
```python
r'アカウントID,ニックネーム\s*\n(.+?)(?=\n\n|---|\Z)'
```

**抽出ロジック:**
```python
import random
import csv
from io import StringIO

# CSV全体を抽出
x_sample_csv_match = re.search(r'アカウントID,ニックネーム\s*\n(.+?)(?=\n\n|---|\Z)', bulk_text, re.DOTALL)
if x_sample_csv_match:
    csv_content = x_sample_csv_match.group(1).strip()
    csv_lines = [line for line in csv_content.split('\n') if line.strip()]
    
    # 3つ以上あればランダム選択、少なければすべて使用
    selected_lines = random.sample(csv_lines, min(3, len(csv_lines)))
    
    # CSV解析して保存
    for idx, line in enumerate(selected_lines, 1):
        reader = csv.reader(StringIO(line))
        row = next(reader)
        st.session_state[f'parsed_x_sample_id_{idx}_{cast_id}'] = row[0]
        st.session_state[f'parsed_x_sample_name_{idx}_{cast_id}'] = row[1]
```

**CSVフォーマット例:**
```csv
アカウントID,ニックネーム
Honne_no_Maruko,本音のまる子
Uragawa_no_Sazae,裏側のサザエさん
Kimama_na_Heidi,気ままなハイジ
Guchi_no_Moomin,愚痴のムーミン
```

**動作:**
- リストから**毎回ランダムに3つ**を選択
- 抽出ボタンを押すたびに異なる組み合わせになる可能性がある
- UIフィールドに自動反映され、保存ボタンでDBに保存

---

### サンプルプロフィール抽出

**パターン1: 太字形式（優先）**
```python
r'\*\*プロフィール:\*\*\s*\n(.+?)(?=\n\n###|---|\Z)'
```

**パターン2: 通常形式（フォールバック）**
```python
r'プロフィール:\s*\n(.+?)(?=\n\n|---|\Z)'
```

---

### 運営ミッション抽出

**パターン1: コロン形式（明示的ラベル）**
```python
r'###?\s*運営ミッション:\s*\n(.+?)(?=\n\n###?|---|\Z)'
```

**パターン2: 太字なし章立て形式**
```python
r'###\s*私たちのミッション.+?\n\n(.+?)(?=\n###\s*第|---|\Z)'
```
- `### 私たちのミッションと3つの原則` に対応

**パターン3: 太字あり章立て形式**
```python
r'###?\s*\*\*私たちのミッション.+?\*\*\s*\n(.+?)(?=\n\n?\*\*プロフィール|\n###?\s*第|---|\Z)'
```
- `### **私たちのミッションと3つの原則**` に対応

---

### ペルソナ設計意図（第1章）抽出

**パターン1: コロン形式（明示的ラベル）**
```python
r'###?\s*ペルソナ設計意図:\s*\n(.+?)(?=\n\n###?|---|\Z)'
```

**パターン2: 太字なし章立て形式**
```python
r'###?\s*第1章.+?\n(.+?)(?=\n###?\s*第|---|\Z)'
```
- `### 第1章：私たちのペルソナと第一印象の設計` に対応

**パターン3: 太字あり章立て形式**
```python
r'###?\s*\*\*第1章.+?\*\*\s*\n(.+?)(?=\n###?\s*\*\*第|---|\Z)'
```
- `### **第1章：私たちのペルソナと第一印象の設計**` に対応

---

### コンテンツ戦略（第2章）抽出

**パターン1: コロン形式（明示的ラベル）**
```python
r'###?\s*コンテンツ戦略:\s*\n(.+?)(?=\n\n###?|---|\Z)'
```

**パターン2: 太字なし章立て形式**
```python
r'###?\s*第2章.+?\n(.+?)(?=\n###?\s*第|---|\Z)'
```
- `### 第2章：私たちのコンテンツ戦略` に対応

**パターン3: 太字あり章立て形式**
```python
r'###?\s*\*\*第2章.+?\*\*\s*\n(.+?)(?=\n###?\s*\*\*第|---|\Z)'
```
- `### **第2章：私たちのコンテンツ戦略**` に対応

---

### 最終目標（第3章）抽出

**パターン1: コロン形式（明示的ラベル）**
```python
r'###?\s*最終目標:\s*\n(.+?)(?=\n\n###?|---|\Z)'
```

**パターン2: 太字なし章立て形式**
```python
r'###?\s*第3章.+?\n(.+?)(?=\n###?\s*\*\*|---|\Z)'
```
- `### 第3章：私たちが目指すゴール` に対応

**パターン3: 太字あり章立て形式**
```python
r'###?\s*\*\*第3章.+?\*\*\s*\n(.+?)(?=\n###?\s*\*\*|---|\Z)'
```
- `### **第3章：私たちが目指すゴール**` に対応

---

### サンプル投稿CSV抽出

**正規表現:**
```python
r'Category,Post[_\s]?Content\s*\n(.*?)(?=\n\n---|\n\n##|\Z)'
```

**対応するヘッダー形式:**
- `Category,Post_Content` （アンダースコア）
- `Category,Post Content` （スペース）
- `Category,PostContent` （区切りなし）

**CSV行の処理:**
- 各行末尾の空白を削除（`rstrip()`）
- 空行をスキップ
- `csv.reader()` でパース（ダブルクォート内のカンマに対応）

---

## 📊 データフロー

```
アカウント運営指針テキスト
    ↓
[正規表現で抽出]
    ↓
セッションステート保存
    ↓
フォーム自動入力
    ↓
ユーザー確認・編集
    ↓
「更新」ボタンクリック
    ↓
データベース保存
```

---

## 🛠️ トラブルシューティング

### 抽出されない場合のチェックポイント

#### 1. ペルソナCSVが抽出されない

**チェック項目:**
- ヘッダー行が `ID,名前,年齢,...` で始まっているか
- データ行のIDが英数字か（`1`, `P001`, `CAST001` など）
- CSV行の後に空行（`\n\n`）または区切り線（`---`）があるか
- ダブルクォートで囲む必要がある値（カンマを含む）は正しく囲まれているか

**修正例:**
```csv
❌ NG: ID,名前,年齢,...
        あいうえお,田中,30,...  ← IDが日本語

✅ OK: ID,名前,年齢,...
       P001,田中,30,...  ← IDが英数字
```

#### 2. 運営指針セクションが抽出されない

**チェック項目:**
- 見出しの形式:
  - `###` の数（2つ `##` または 3つ `###`）
  - `**` の有無（太字か通常か）
  - `:` の有無（コロン付きか）
- 見出しと本文の間に空行（`\n\n`）があるか
- 次のセクションまでの区切り:
  - 次の `###` 見出し
  - 区切り線 `---`
  - ファイル末尾

**対応形式一覧:**
```markdown
✅ OK: ### 運営ミッション:
✅ OK: ### 私たちのミッションと3つの原則
✅ OK: ### **私たちのミッションと3つの原則**
✅ OK: ## **第1章：...**
✅ OK: ### 第1章：...
```

#### 3. サンプルプロフィールが抽出されない

**チェック項目:**
- `プロフィール:` の後にコロン（`:`）があるか
- 太字形式なら `**プロフィール:**` と正しくエスケープされているか
- プロフィール文の後に空行（`\n\n`）または次の見出し（`###`）があるか

**修正例:**
```markdown
❌ NG: プロフィール  ← コロンがない
       テキスト

✅ OK: **プロフィール:**  ← 太字+コロン
       テキスト
```

#### 4. サンプル投稿CSVが抽出されない

**チェック項目:**
- ヘッダー行が以下のいずれかか:
  - `Category,Post_Content`（アンダースコア）
  - `Category,Post Content`（スペース）
  - `Category,PostContent`（区切りなし）
- 投稿内容（カンマを含む）はダブルクォートで囲まれているか
- CSV終端に空行2つ（`\n\n`）+ 区切り線（`---`）または次の見出し（`##`）があるか

**修正例:**
```csv
❌ NG: Category,Post Content
       告白録,今日、彼に会った  ← クォートなし、カンマでパース失敗

✅ OK: Category,Post_Content
       "告白録","今日、彼に会った"  ← クォートあり
```

#### 5. 文字エンコーディング

**チェック項目:**
- ファイルがUTF-8エンコーディングであること
- BOM（Byte Order Mark）がないこと

**確認方法:**
```bash
file -i your_file.md
# 出力: text/plain; charset=utf-8
```

---

## 📅 更新履歴

- **2025-10-27**: 初版作成
  - ペルソナCSV 14フィールド対応
  - 主なフォロー対象フィールド追加
  - 運営指針4セクション対応（3段階フォールバック処理）
  - サンプルプロフィール・投稿対応（複数パターン）
  - 英数字ID対応（`P001`, `CAST001`など）
  - トラブルシューティングセクション追加

---

## 🎯 実装ポイントまとめ

### フォールバック処理の仕組み

すべての抽出パターンは**柔軟性を重視した3段階フォールバック**を実装：

1. **明示的ラベル形式** → 最も確実（例: `### 運営ミッション:`）
2. **章立て形式（太字なし）** → 一般的（例: `### 第1章：...`）
3. **章立て形式（太字あり）** → 装飾版（例: `### **第1章：...**`）

### CSV処理の工夫

- **ID**: 数字だけでなく英数字に対応（`\d+` → `[A-Za-z0-9]+`）
- **区切り**: スペース・アンダースコア・なしすべてに対応（`Post[_\s]?Content`）
- **カンマ**: `csv.reader()`でダブルクォート内のカンマを正しくパース
- **空行**: `rstrip()`と`if line.strip()`で余分な空行を除去

### セッションステート管理

- 抽出データは即座にDBに保存せず、まずセッションステートに保存
- ユーザーがフォームで確認・編集可能
- 「更新」ボタンでDB保存（ユーザーコントロール重視）

### エラーハンドリング

- 新規カラム（`main_follow_targets`, `x_sample_id_*`, `x_sample_name_*`）は`try-except`で存在チェック
- `sqlite3.Row`オブジェクトの制限（`.get()`不可）に対応
- 抽出失敗時は次のパターンへフォールバック（エラー非表示）

---

## 📅 更新履歴

### 2025-10-27: Xアカウントサンプル（ランダム3つ選択）機能追加
- **新規フィールド追加:**
  - `x_sample_id_1`, `x_sample_name_1`
  - `x_sample_id_2`, `x_sample_name_2`
  - `x_sample_id_3`, `x_sample_name_3`
- **抽出ロジック:**
  - `アカウントID,ニックネーム`のCSVリストから`random.sample()`で3つランダム選択
  - 毎回の抽出で異なるアカウントが選ばれる
- **UIフィールド:**
  - 「🎲 Xアカウントサンプル（ランダム3つ選択）」セクション追加
  - 3組のID/ニックネームフィールド（自動反映）
- **セッションステート管理:**
  - Streamlit `text_input`のkeyとvalueの競合を解決
  - `parsed_*`から`edit_*`へのコピーで自動反映を実現
- **データベース:**
  - `persona_detailed`テーブルに6カラム追加（ALTER TABLE）
  - UPDATE/INSERTクエリに新フィールドを追加

### 2025-10-27以前
- ペルソナCSV抽出機能（英数字ID対応）
- サンプルプロフィール抽出（太字対応）
- 運営ミッション・章立て抽出（3段階フォールバック）
- サンプル投稿CSV抽出・一括インポート
- 主なフォロー対象フィールド追加
- 完全ドキュメント作成

