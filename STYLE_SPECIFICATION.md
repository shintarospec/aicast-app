# AIcast Room スタイル仕様書

## 概要
AIcast Roomのサイバーパンク調デザインシステムの仕様をまとめたドキュメントです。

---

## カラーパレット

### メインカラー
- **プライマリカラー**: `#00FFFF` (シアン)
- **セカンダリカラー**: `#00CED1` (ダークターコイズ)
- **背景色**: `#000000` (黒)
- **サイドバー背景**: `#1a1a1a` (ダークグレー)

### アクセントカラー
- **テキスト白**: `#FFFFFF`
- **グレー**: `#888888` (前回投稿など)
- **ダークスレート**: `#2F4F4F` (ホバー時など)

---

## コンポーネント別スタイル

### 1. サイドバー

#### ロゴ
- **発光アニメーション**: あり（3秒周期）
- **text-shadow**: 
  - 0%/100%: `0 0 20px rgba(0,255,255,1), 0 0 40px rgba(0,255,255,0.8), 0 0 60px rgba(0,255,255,0.5)`
  - 50%: `0 0 30px rgba(0,255,255,1), 0 0 50px rgba(0,255,255,1), 0 0 80px rgba(0,255,255,0.8)`
- **フォントサイズ**: `1.8em`
- **カラー**: `#00FFFF`

#### メニュー（option_menu）
- **通常状態**:
  - 背景: 透明
  - テキスト: `#FFFFFF`
  - border-radius: `8px`

- **選択状態**:
  - 背景: `linear-gradient(135deg, #00CED1 0%, #00FFFF 100%)`
  - テキスト: `#000000` (黒文字)
  - フォントウェイト: 通常（太字なし）

- **ホバー状態**:
  - 背景: `linear-gradient(135deg, #00CED1 0%, #00FFFF 100%)`
  - transform: `scale(1.02)`
  - transition: `all 0.3s ease`

---

### 2. ボタン

#### 通常ボタン
- **背景**: `linear-gradient(135deg, #00CED1 0%, #00FFFF 100%)`
- **テキストカラー**: `#000000`
- **border**: なし
- **border-radius**: `8px`
- **フォントサイズ**: `16px`
- **フォントウェイト**: `bold`
- **パディング**: `10px 24px`
- **transition**: `all 0.3s ease`

#### ボタンホバー効果
- **transform**: `scale(1.05)` (5%拡大)
- **box-shadow**: 
  ```css
  0 0 40px rgba(0,255,255,1),
  0 0 60px rgba(0,255,255,0.8),
  0 0 80px rgba(0,255,255,0.5)
  ```

#### プライマリボタン（type="primary"）
- **背景**: `linear-gradient(135deg, #00FFFF 0%, #00CED1 100%)` (通常と逆)
- **その他**: 通常ボタンと同じ

#### プライマリボタンホバー効果
- **transform**: `scale(1.05)`
- **box-shadow**: より強力な発光
  ```css
  0 0 50px rgba(0,255,255,1),
  0 0 70px rgba(0,255,255,0.9),
  0 0 90px rgba(0,255,255,0.6)
  ```

#### フォーム送信ボタン（form_submit_button）
- プライマリボタンと同じスタイル
- CSSセレクター: 
  - `button[kind="primary"]`
  - `button[data-testid="baseButton-primary"]`
  - `.stForm button`
  - `form button[type="submit"]`

---

### 3. タブメニュー

#### 選択されたタブ
- **背景**: 透明
- **テキストカラー**: `#00CED1`
- **border-bottom**: `1px solid #00CED1` (細いアンダーライン)
- **フォントウェイト**: `bold`

#### 未選択タブ
- **テキストカラー**: `#FFFFFF`
- **背景**: 透明

#### タブホバー
- **背景**: `#2F4F4F`
- **テキストカラー**: `#00FFFF`

---

### 4. プルダウンメニュー

#### セレクトボックス
- **背景**: `#2F4F4F`
- **ボーダー**: `1px solid #00CED1`
- **border-radius**: `6px`
- **テキストカラー**: `#00CED1`

#### ポップオーバー（選択肢リスト）
- **背景**: `#2F4F4F`
- **ボーダー**: `1px solid #00CED1`
- **border-radius**: `6px`
- **box-shadow**: `0 4px 12px rgba(0,255,255,0.3)`

#### 選択肢のホバー
- **背景**: `#00FFFF`
- **テキストカラー**: `#000000`

---

### 5. タイトル・見出し

#### H1
- **フォントサイズ**: `1.28em` (デフォルトの80%)
- **カラー**: `#00CED1`

#### H2
- **フォントサイズ**: `1.04em` (デフォルトの80%)
- **カラー**: `#00CED1`

#### H3
- **フォントサイズ**: `0.88em` (デフォルトの80%)
- **カラー**: `#00CED1`

---

## ボタンラベル仕様

### 投稿管理 > 投稿案
| 機能 | ラベル | type | 備考 |
|------|--------|------|------|
| 調整 | 🔧 調整 | - | 投稿内容を編集 |
| 承認 | ✅ 承認 | primary | 承認処理 |
| 却下 | ❌ 却下 | - | 却下処理 |

### リツイート予約
| 機能 | ラベル | type | 備考 |
|------|--------|------|------|
| 予約作成 | 📅 リツイート予約を作成 | primary | フォーム送信 |
| 今すぐ実行 | ⚡ 今すぐ実行 | primary | 即時実行 |
| 再スケジュール | 🔄 再スケジュール実行 | primary | 再設定 |
| 引用再作成 | 🔄 引用ツイートとして再作成 | primary | 変換 |

---

## 設定ファイル

### config.toml (.streamlit/config.toml)
```toml
[theme]
primaryColor = "#00FFFF"
backgroundColor = "#000000"
secondaryBackgroundColor = "#000000"  # 重要：黒に設定（シアン背景の漏れを防止）
textColor = "#FFFFFF"
font = "sans serif"
```

### style.css
メインスタイルシート。以下のセクションで構成：
1. サイドバー背景
2. option_menuスタイル
3. タイトル・見出し
4. タブメニュー
5. プルダウンメニュー
6. ボタン（通常/プライマリ/フォーム）

---

## 重要な注意事項

### シアン背景の漏れ対策
**問題**: `config.toml`の`secondaryBackgroundColor`が`#2F4F4F`だと、サイドバーやウィジェットにシアン寄りの色が漏れて見える。

**解決策**: `secondaryBackgroundColor = "#000000"`に設定する。

### ボタンのスタイル適用
- 通常の`st.button`はCSSで自動適用
- `st.form_submit_button`は複数セレクターで強制適用が必要
- `type="primary"`を指定すると、より強い発光エフェクト

### フォントサイズ
- タイトル類はデフォルトの80%に縮小（視認性向上のため）
- ボタンは`16px`固定

---

## デザインコンセプト

### サイバーパンク調
- 黒背景 + シアン発光
- グラデーション背景
- ホバー時のスケールアップ & 発光エフェクト
- アニメーション（ロゴの発光）

### ユーザビリティ
- 重要なアクションは`type="primary"`で視覚的に強調
- 絵文字で機能を直感的に表現
- ホバーエフェクトでインタラクティブ性を向上

---

## 更新履歴

### 2025-10-30
- 初版作成
- サイドバー背景を`#1a1a1a`に変更（明るさ向上）
- タブアンダーラインを`3px`→`1px`に細く
- ロゴの発光を強化
- 全ボタンにサイバーパンク調デザイン適用
- 投稿案ボタンに絵文字追加（調整/承認/却下）
- `config.toml`の`secondaryBackgroundColor`を黒に変更（シアン漏れ対策）

### 2025-11-01（最終完了版）
- **option_menuスタイル最終調整**:
  - メニューボックス: 黒背景 + 15px角丸をapp.py内で直接指定
  - 選択時のフォントカラー: 黒文字で視認性向上
  - 外部CSS（style.css）では効果なしと判明 → app.py内stylesパラメータで対応
- **フォーム背景色の最適化**:
  - `config.toml`の`secondaryBackgroundColor`を`#1a1a1a`に変更
  - フォームの枠線が視認可能になり、ユーザビリティ向上
- **技術的教訓**:
  - streamlit-option-menuは外部CSSセレクタが効きにくい仕様
  - app.py内stylesパラメータでの直接指定が確実
  - config.toml secondaryBackgroundColorはフォーム・ウィジェット背景に影響

---

## option_menuスタイル詳細（最終版）

### コンテナ（メニューボックス）
```python
"container": {
    "padding": "0.5rem",
    "background-color": "#000000",
    "border-radius": "15px"
}
```

### 選択時のメニュー項目
```python
"nav-link-selected": {
    "color": "#000000"  # 黒文字（シアングラデーション背景とのコントラスト）
}
```

### スタイル適用場所
- **ファイル**: `app.py` (2689-2703行目)
- **関数**: `main()`内のサイドバーメニュー定義
- **理由**: style.cssでの外部指定は効果なし。app.py内で直接指定が必須。

---

## トラブルシューティング追記

### option_menuが表示されない / メインカラムに移動してしまう
**原因**: `with st.sidebar:`ブロックのインデントが正しくない

**解決策**:
```python
with st.sidebar:
    st.markdown("...")  # 正しくインデント
    
    selected_page = option_menu(...)  # option_menuもwith内に配置
```

### option_menuのスタイルが反映されない
**原因**: 外部CSS（style.css）のセレクタが効かない仕様

**解決策**: app.py内のstylesパラメータで直接指定する（現在の実装参照）

### フォームの枠線が見えない
**原因**: `secondaryBackgroundColor`が`#000000`（黒）で、背景と同化

**解決策**: `secondaryBackgroundColor = "#1a1a1a"`に変更（現在の設定）

````
